window.mermaid = window.mermaid || {
  initialize: function () {},
  run: function () {
    document.querySelectorAll('.mermaid').forEach(function (node) {
      node.setAttribute('data-rendered', 'fallback');
    });
  }
};
